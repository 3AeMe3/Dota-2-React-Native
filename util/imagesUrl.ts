export function imageHero(path: string) {
  return `https://cdn.cloudflare.steamstatic.com/${path}`;
}

export function imageItem(item: string) {
  return `https://cdn.cloudflare.steamstatic.com/apps/dota2/images/dota_react/items/${item}.png`;
}
